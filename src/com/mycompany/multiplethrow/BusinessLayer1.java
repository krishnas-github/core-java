package com.mycompany.multiplethrow;

import com.mycompany.exceptionhandlingthrow.UILayer;

public class BusinessLayer1 {
   /* public String aCheck(int age) throws ULayer {
        if(age<18){
            ULayer be = new ULayer("invalid age to vote");
            throw be;
        }
        else{
            return "Valid age to vote";
        }
    }

    /*public String mCheck(int age) throws BusinessException1{
        if(age<21){
            BusinessException1 be = new BusinessException1("invalid age to marry");
            throw be;
        }
        else{
            return "Valid age to marry";
        }
    }*/


}
